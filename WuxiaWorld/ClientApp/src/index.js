import React from 'react';
import ReactDOM from 'react-dom';
import TerminalContainer from './app/containers/terminal.container';

ReactDOM.render(<TerminalContainer />, document.getElementById('main'));
